#include "NukeWave.h"

void NukeWave::StartWave(const WaveData& waveData)
{
    //std::cout << "START NUKE WAVE";
    //_amount = waveData.amount;

    //// This will have to be changed
    //float spacingX = 150.0f;
    //float startX = (RM->WINDOW_WIDTH / 2.0f) - 350.0f;
    //float startY = RM->WINDOW_HEIGHT - 50.0f;

    //for (int i = 0; i < _amount; i++)
    //{
    //    float posX = startX + i * spacingX;

    //    _positions.push_back(Vector2(posX, startY));
    //    Nuke* nuke = new Nuke(_positions[i]);

    //    _enemies.push_back(nuke);
    //    SPAWNER.SpawnObject(nuke);
    //    nuke->Start();
    //}
}